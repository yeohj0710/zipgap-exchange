# 실거래가 채우기 — 상시 작업 지시서

이 문서를 Codex 세션에 그대로 넣고 돌린다. 한 달 내내 같은 문서를 쓴다.

```
C:\dev\zipgap-exchange 에서 CODEX.md 를 읽고 그대로 따라라.
멈추지 말고 계속 돌려라. 한 사이클이 끝나면 다음 사이클을 시작해라.
```

---

## 하는 일 한 줄

`data/listings.json` 의 `source: "seed"` 를 국토교통부 실거래 원자료로 계산한
`source: "real"` 로 하나씩 바꾼다. 코드는 건드리지 않는다.

지금 상태는 `npm run data:next -- --stats` 로 본다.

---

## 준비물

법정동코드 표(`data/lawd-codes.json`)는 이미 들어 있다. 시군구 261개, 읍면동 5,052개다.
새로 만들 일이 있으면 `node scripts/build-lawd-codes.mjs <파일>` 을 쓴다.

실거래 원자료를 받으려면 인증키가 하나 필요하다. 공공데이터포털(data.go.kr)에서
**국토교통부 아파트 매매 실거래가 상세 자료**를 신청해 받은 키를 `.env.data` 에 적는다.

```
MOLIT_SERVICE_KEY=받은키
```

**키가 없다고 멈추지 마라.** `npm run data:next` 가 알아서 키 없이 할 수 있는 일만 내준다.
단지 조회 열쇠 맞추기, 지수 구성 채우기, 세대수·준공년도 채우기, 올릴 단지 후보 쌓기가
전부 키 없이 되는 일이고, 한동안 이것만 해도 할 일이 남는다. 키가 들어오면 다음
사이클부터 받아오는 일이 저절로 목록에 뜬다.

사람에게 물어야 할 때는 딱 하나다. **키 없이 할 일이 다 떨어졌을 때**
(`data:next` 가 `[idle]` 을 내줄 때) 한 번 알리고, 그 뒤로는 후보를 더 쌓는다.

---

## 한 사이클

### 1. 할 일을 받는다

```bash
npm run data:next -- --count 5
```

무엇을 할지 스스로 정하지 마라. 이 명령이 시키는 것만 한다. 순서에는 이유가 있다.
조회 열쇠가 없으면 자료를 못 받아 오므로 그것부터 시킨다.

### 2. 시킨 일을 한다

일감 앞에 붙은 `[말머리]` 가 무슨 일인지 알려 준다.

| 말머리 | 하는 일 | 키 필요 |
| --- | --- | --- |
| `resolve-lawd` | `node scripts/resolve-lawd.mjs` 로 단지의 법정동코드를 맞춘다. 보류로 나오면 `region` 을 법정동 표기대로 고치고 `--write` | 아니오 |
| `resolve-index` | 지수 구성 시군구를 `scripts/resolve-lawd.mjs` 의 `INDEX_RULES` 에 적고 `--write` | 아니오 |
| `fill-meta` | 세대수·준공년도를 채운다 | 아니오 |
| `scout` | 올릴 단지 후보를 `data/wishlist.json` 에 쌓는다. 넣고 나서 `resolve-lawd.mjs` 로 `region` 표기를 확인한다 | 아니오 |
| `add-asset` | 후보를 종목으로 올린다 | 아니오 |
| `resolve-apt` | 실거래를 조회해 아파트명 원문을 확정한다 | 예 |
| `fill-month` | 그 달 실거래를 받아 시세를 계산한다 | 예 |

**받아오는 호출은 네가 직접 한다.** 스크립트에 넣지 마라. 스크립트는 할 일 목록과 검증만 맡는다.

- 엔드포인트와 파라미터 이름은 **data.go.kr 의 해당 서비스 문서에서 그때 확인한다.**
  주소가 바뀐 적이 있다. 외워 둔 주소를 그대로 쓰지 마라.
- 조회 단위는 (시군구 법정동코드 5자리 × 계약년월 YYYYMM) 이다.
- 받은 원본은 `data/raw/{lawdCd}-{ym}.json` 으로 **반드시 저장한다.**
  같은 것을 두 번 받지 않기 위해서다. 개발계정은 하루 호출 수가 정해져 있다.
  파일이 이미 있으면 호출하지 말고 그걸 읽어라.

### 3. 값을 계산한다

`data/SCHEMA.md` 의 규칙을 그대로 따른다. 요약하면,

- 단지형: 전용면적이 `unitArea` ± 1.5m² 안인 거래만 골라 **중앙값**. 평균이 아니다.
- 거래금액은 만원 단위 정수로 넣는다. 콤마를 지우고 숫자만 남긴다.
- 그 달 거래가 0건이면 **손대지 않는다.** `seed` 로 그냥 둔다.
- 층·향 보정은 하지 않는다.
- 해제된 거래(거래취소)가 표시되어 있으면 뺀다.

### 4. 검증하고 커밋한다

```bash
npm run verify:data
```

통과하지 못하면 커밋하지 않는다. 오류를 고치고 다시 돌린다.

통과하면 커밋한다. 한 번에 하나씩 말고 **5~20건씩 묶어서** 커밋한다.

```
데이터: 은마아파트 2024-01~2024-06 실거래 반영
```

### 5. 진행 상황을 적는다

`data/PROGRESS.md` 맨 위에 한 줄 더한다.

```
- 2026-08-14 03:12 · EUNMA 6개월 · 남은 칸 512
```

### 6. 다시 1번으로

---

## 절대 하면 안 되는 것

1. **`symbol` 을 바꾸거나 지우지 않는다.** 사람들이 그 종목을 이미 갖고 있다.
   이름이 틀렸으면 `name` 만 고친다. `data/symbols.lock.json` 이 이걸 지킨다.
2. **`shareDivisor` 를 바꾸지 않는다.** 바꾸면 모든 사람의 평가액이 한순간에 튄다.
3. **3개월보다 오래된 `price` 를 고치지 않는다.** 이미 그 값으로 거래가 일어났다.
   원자료가 정정되었으면 최근 3개월 안에서만 고친다.
4. **추정치에 `source: "real"` 을 붙이지 않는다.** 이 표시가 사이트의 "표본값" 배너를 끈다.
   못 받아 온 달은 `seed` 로 두는 게 맞다. 비어 있는 것은 흠이 아니다.
5. **`lib/`, `app/`, `components/` 를 건드리지 않는다.** 거래 로직은 네 일이 아니다.
   고쳐야 할 게 보이면 `data/PROGRESS.md` 에 적어 두고 넘어가라.
6. **유료 API 를 쓰지 않는다.** 여기서 쓰는 자료는 전부 무료 공공데이터다.
   돈이 드는 호출이 필요해 보이면 멈추고 사람에게 물어라.
7. **크롤링으로 우회하지 않는다.** 공식 API 로 안 되는 자료는 안 되는 것으로 둔다.

---

## 자료 출처

| 무엇 | 어디 | 비고 |
| --- | --- | --- |
| 아파트 매매 실거래 | 공공데이터포털 · 국토교통부 아파트 매매 실거래가 상세 자료 | 주력. 단지·면적·금액·계약일이 다 나온다 |
| 법정동코드 | 행정표준코드관리시스템 | 시군구 5자리 |
| 아파트 매매가격지수 | 한국부동산원 R-ONE | 지수형 종목 교차 확인용 |
| 단지 기본정보(세대수·준공년도) | 공공데이터포털 · 공동주택 단지 목록 | `households`, `builtYear` 채울 때 |

전세·월세 실거래를 받을 수 있으면 `rentYield` 도 실측으로 바꾼다.
전세가와 매매가 비율에서 전월세전환율을 적용해 연 수익률을 낸다.
지금 들어 있는 `rentYield` 는 전부 추정치다.

---

## 한 달 로드맵

작업 순서는 `data:next` 가 알아서 정한다. 아래는 대략 어디까지 가면 좋은지다.

| 주 | 목표 |
| --- | --- |
| 1주 | 단지 12개의 조회 열쇠 확정, 최근 12개월 실거래 반영 |
| 2주 | 32개월 전체를 실거래로 채우고 지수 6종을 실거래 기반으로 재계산 |
| 3주 | 단지를 50개까지 늘린다. 서울 쏠림을 줄이고 광역시·도청소재지를 넣는다 |
| 4주 | 100개까지 늘리고, 전월세 자료로 `rentYield` 를 실측으로 바꾼다 |

단지를 고르는 기준은 이렇다.

- 거래가 꾸준히 있는 곳. 1년에 5건도 안 되는 단지는 값이 안 움직여 재미가 없다.
- 이름이 알려진 곳. 사람들이 알아야 사고판다.
- 지역이 겹치지 않게. 강남구만 열 개 넣지 마라.

종목이 50개를 넘어가면 `data/listings.json` 이 커져서 브라우저가 받는 양도 늘어난다.
100개를 넘길 때가 되면 사람에게 알려라. 목록과 시세를 나눠야 한다.

---

## 상태 파일

| 파일 | 무엇 |
| --- | --- |
| `data/listings.json` | 산출물. 사이트가 읽는 유일한 원장 |
| `data/symbols.lock.json` | 지우면 안 되는 종목 목록 |
| `data/wishlist.json` | 올릴 단지 후보 |
| `data/raw/` | 받아 온 원자료 캐시. 같은 것을 두 번 받지 않으려고 둔다 |
| `data/lawd-codes.json` | 법정동코드 표. 시군구 5자리가 실거래 조회 열쇠다 |
| `data/PROGRESS.md` | 무엇을 언제 했는지 |
| `data/SCHEMA.md` | 규격. 헷갈리면 여기를 본다 |

## 쓰는 명령

```bash
npm run data:next -- --count 5   # 다음에 할 일
npm run data:next -- --stats     # 진행률만
node scripts/resolve-lawd.mjs            # 법정동코드 맞춰 보기
node scripts/resolve-lawd.mjs --write    # 맞은 것만 써 넣기
npm run verify:data              # 커밋 전 검사. 통과 못 하면 커밋하지 않는다
```
